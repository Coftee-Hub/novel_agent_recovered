# 待办清单

2026-08-22 逐条核过代码后整理。分档标准是**不做会怎样**，不是工作量。

---

## 一、挡在路上的

### 1. Phase 7 的验收从没做过

方案定的验收标准是「连续自动跑 5 章，state 无漂移、无重复桥段」。至今全是一次一章。
checkpointer 已经接进 `write --graph`，但**没跑过 5 章连写**。
跑完这一关再决定 `--graph` 要不要变成默认路径（目前默认仍是直调 pipeline）。

### 2. judge 阈值该按实际分布调了

24/35 是方案里写明的"拍脑袋起始值，跑 10 章后按实际分布调"。

**记录机制已经就位**（2026-08-22）：每次评审写一行 `book/judgments.jsonl`，
`novel-agent judgments` 看分布。此前分数跑完就丢，5 次评审一个数据点没留下。
现在有 1 行（第 3 章 31/35，从 checkpoint 回捞），跑到第 10 章再调。

怎么读分布见 HANDOFF 的「judge 评分」一节。要点：**某一维反复低分该改 skills
或细纲，不是调低阈值** —— 降标准让稿子通过是自欺。

---

## 二、不做会持续付代价的

### 5. DeepSeek 没填计价表，成本一直少算

`judge` / `archivist` / `extractor` 三个角色的调用在 `run_log.jsonl` 里成本全记 0
（`router._prices()` 查不到就返回 0）。全书成本 $42 的外推只算了 Claude 那部分。
填 `config/models.yaml` 的 `pricing.deepseek-v4-flash` 即可。

### 6. 修订环对"细纲与文风目标打架"的章节收敛不了

第 3 章实证：首轮心理密度 10.4（下限 28）、短句率 32.3%（上限 26%）；
修订一轮后心理修到 44.4、短句率 13.9%，**同时**句长中位从 18 涨到 32（上限 27）、
长句率涨到 54.6%（上限 48%）、多出三个超长段落。给了完整文风剖面也照样打地鼠。

根因不在 writer 而在细纲（禁内心 + 堆动作）。两轮上限撑不住这种情况。
可能的解法：修订两轮仍不过时，**回到 architect 改细纲重来**，而不是直接落
`needs_human`。这条路径现在不存在。

### 7. DeepSeek 只是结构化角色在用，正文没试过

`deepseek-v4-pro` 存在但没测过文笔。值得花约 $0.05 写一场、跟第 1-3 章跑同一套
文风指纹对比 —— 有数据才谈得上要不要换主力，或者至少把它加成 writer/stitcher 的
**末位兜底**（现在两个 fallback 都是同一家中转站的池子，一起挂过好几次）。

### 8. 第 2 章的文风在上沿，再往上就该压

叙述句长中位 26（上限 27）、长句率 43.1%（上限 48%）。第 1 章是 20 / 27.6%。
两章样本看不出是趋势还是波动，第 3、4 章出来后对一下。

### 9. RAG 的 Phase 6 验收没做完

「缓存命中率不下降」这条**从没在 RAG 开启的状态下测过**，因为检索质量差、
默认关掉了（BM25 对具象词有效，对抽象情绪无能为力，而场景规格里最有区分度的
恰恰是情绪）。换 embedding 只需替换 `PassageIndex.score()` 一个方法。

---

## 三、可以做，不急

### 10. `scripts/` 里有两个和 CLI 重复的脚本

`plan_volume.py`、`write_chapter.py` 的功能已经被 `novel-agent plan` / `write` 收口。
留着会漂移（`write_chapter.py` 里那份手写死的假卷大纲就是这么来的）。

### 11. `archive` 命令重出的细纲不落盘

它会为了归档重出一次章细纲，但不存 —— 存了又会覆盖掉真正产出这一章的那份。
现状是有意为之，但那份重出的细纲花了钱，扔掉可惜。

### 12. LangGraph 的 `interrupt()` 没用上

方案里卷大纲确认要走 `interrupt()`，现在靠的是"`plan` 命令跑完打印出来给人看、
人满意再跑 `write`"。效果一样，但没有做成图里的断点。

### 13. `checkpoints.sqlite` 没有清理策略

每章一条线程，细纲改版还会分出新线程。跑到 140 章时会攒下不少，
目前没有任何回收机制。

### 14. Web UI

方案第十节明确写了先纯 CLI，跑顺了再考虑（大纲确认环节最值得图形化）。

---

## 已经做完的，别重复做

- **卷级摘要压缩**（2026-08-22）：`Archivist.compress_volume()` + 卷末触发
  （两条路径都接）+ `novel-agent compress <卷>` 补做 + 上下文规则从"最近 10 章"
  改成"尚未被卷梗概覆盖的全部章节 + 往卷梗概"+ writer 也读得到往卷梗概。
  21 个测试。细节见 HANDOFF 的「两层摘要」与「六条最容易被误改的设计」第 5 条
- **第 3 章成稿**（2026-08-22，3,910 字，gate 全项通过，关系推到「好感」）
- 缝合改流式（解决四次连崩）、ASCII 引号机械规范化、修订指令带原文片段、
  checkpoint 线程不再继承上一轮的修订计数、归档失败不再丢成稿
- 章细纲落盘 + `outline` 命令 + `--reuse-outline`
- 场景草稿落盘 + `--resume-drafts`
- LangGraph checkpoint 接进 CLI（`--graph`）+ 细纲变更防呆
- 大纲/成稿覆盖前自动归档到 `_versions/`
- ASCII 引号导致「对话占比 0.0%」的误判
- aws-q 补 403 重试
- `ch_013.md` 测试残留已删
