# 交接说明

给新会话（或几个月后的自己）看的。**代码注释里已经记了每个决策的理由**，
这里只放那些散落在各处、单看一个文件看不出来的东西。

---

## 这是什么

《落款》——一部现代言情小说的自动写作流水线。大学校园 → 毕业过渡 → 职场，
目标 30-50 万字。作者只做两件事：**提供模板小说、确认卷级大纲**，其余交给流水线。

作者的定位：这个项目是为了练手，为真正的 agent 项目做准备。所以工程质量本身
就是目标之一，不只是"能跑出小说"。

## 当前进度

| Phase | 状态 |
|---|---|
| 0 LLM 层 / prompt 分层 / 多供应商 | ✓ |
| 1 state schema / gate | ✓ |
| 2 语料萃取 → 6 份 skills | ✓ 已定稿 |
| 立项（人物设定）| ✓ 由人直接写，不交给模型 |
| 3 architect | ✓ 第一卷大纲已出并确认 |
| 4 writer + stitcher | ✓ 第 1 章已成稿并归档 |
| 5 judge / archivist / 修订环 | ✓ 代码完成，闭环已真实跑通 |
| 6 RAG | ✓ 建成但**默认关闭**（实测收益为负）|
| 7 CLI + LangGraph | ⚠️ 见下 |

`book/chapters/` 里第 1-3 章是正式成稿，`story_state.json` 停在第 3 章。
关系已推到「好感」，11 个桥段用掉，debt_001/002/006 已回收。

### 第 3 章的复盘：六次尝试，四个 bug

这一章前后跑了六次才成稿，值得记 —— **四次失败里只有一次真是渠道的错**，
其余三次都是我们自己的代码。

| # | 症状 | 真因 | 修法 |
|---|---|---|---|
| 1 | 连续四次崩在 stitcher | 缝合是单次输出最大的请求（6,000 token / 2-16 分钟），非流式期间连接一个字节不动，上游掐断 | 改流式接收。改前中位 239s、最长 972s 外加三次直接失败；改后 5 次全成功，中位 151s |
| 2 | 「整章对话占比 0.0%」 | 模型吐 ASCII 引号，而对话占比只认全角引号 —— 一整章被判成"没有对话"，三场全部重写 | 生成结果过一遍 `_postprocess`（配对转全角 + 省略号/破折号规范化）。拿实测稿验证：52 处标点错误 → 0 |
| 3 | 修订两轮永远修不掉同一个超长段落 | 修订指令只给了 `f.message`，**把行号和原文片段丢了** —— writer 不知道该拆哪一段 | `_targets` 改用 `str(f)`，并且不再按规则名白名单过滤（漏登记一个规则 = 那类问题永远传不到 writer） |
| 4 | 连着两次跑「修订 2 轮后仍未通过」，实际一轮修订都没做 | **LangGraph 线程状态是累积的**：往一条已经跑完的线程再 invoke，seed 只是合并进旧状态，`revisions` 还停在上次的 2 | `_pick_thread()`：只续没跑完的那条，其余一律新开线程 |

另外两条不算 bug 但同样贵：

- stitcher 会在正文末尾用 `---` 起一段「缝合说明」（列它改了哪几处）。输出是原样
  存盘的，说明就成了小说的一部分。**这类缺陷修订环修不掉** —— 重写场景改变不了
  stitcher 的习惯，每重缝一次就再加一遍。已在 prompt 里禁掉 + 机械剥离 + gate 拦截。
- archivist 连着三次返回不可解析的内容，**整章直接判异常，成稿一个字没落盘** ——
  而那时它已经过了 gate 和 judge。归档是记账，正文是货，记账失败不该把货扔了。
  现在归档失败会照常落盘成稿并提示 `novel-agent archive <章>` 补录。

教训收敛成一条：**凡是"贵的东西已经拿到手、后面还有一个便宜步骤"的地方，
那个便宜步骤失败都不该连累前面。** 缝合、归档、卷末压缩现在都按这条处理。

### Phase 7：checkpoint 已接上，还差一次 5 章连跑

方案第八节给 Phase 7 定的验收标准是「连续自动跑 5 章，state 无漂移、无重复桥段」。

**2026-08-22 之前的状态**：`graph/build.py` 的 `build_graph` + SqliteSaver 只有
`tests/test_graph.py` 在引用，`novel-agent write` 一直直调 `ChapterPipeline.run` ——
方案里「checkpointer：崩溃可续跑」这条能力在实际敲的命令里根本不存在。第 3 章
两次崩在缝合就是代价：流程要从出细纲重走一遍。

**现在**：`write --graph` 走图，checkpoint 落在 `book/checkpoints.sqlite`，
thread_id 是「卷-章」。崩在哪个节点，重跑就从哪个节点接着来（细纲不重出、
场景不重写）。`tests/test_cli_graph.py` 按第 3 章的真实剧本测了这条路：
缝合抛异常 → 重跑 → architect 与 writer 的调用次数都是 1。

旧的 `build_graph(pipeline, checkpoint_db=...)` 返回 `(图, 还没进入的上下文管理器)`
元组，调用方得自己知道要 with 一下再自己 compile —— **没人用它正是因为这个签名**。
已改成 `checkpointed_graph(pipeline, db)` 上下文管理器，拿到手就能 invoke。

**还欠着的**：连续 5 章从没跑过。默认路径仍是直调 pipeline（`--graph` 是显式开关），
等 5 章连跑验收绿了再考虑换默认。

四层保护各管各的，不互相替代：

| 层 | 保住什么 | 崩了 / 改了以后 |
|---|---|---|
| `book/drafts/` 草稿落盘 | 已经花掉的钱（每场正文） | `write --resume-drafts` 只补没写完的场景 |
| `book/checkpoints.sqlite` | 已经走过的流程 | `write --graph` 从崩掉的节点续跑 |
| `_versions/` 旧版归档 | 被覆盖掉的大纲与成稿 | 直接从 `_versions/` 取回 |
| 每章落盘 state | 已经写完的章节 | 重跑从下一章开始 |

### 版本归档（2026-08-22 加）

卷大纲、章细纲、成稿都是"重跑一次就原地覆盖"的文件，而新版未必比旧版好。
现在覆盖前自动留旧版：

- `book/outlines/_versions/ch_003.v01.json` / `.md` —— **v01 存的是被第一次
  覆盖掉的那一版**，序号递增，内容一样时不留（避免重跑攒出一堆无差别副本）
- `book/chapters/_versions/ch_00X.vNN.md` —— 重跑已有成稿的章节时旧稿进这里。
  「未通过不覆盖」那条防线管不到这种情况：新稿是**通过了**的，只是未必更好
- 卷大纲同理。它是全流程唯一经过人确认的产物，最不能丢

**细纲一改版，这一章的旧草稿会自动退休**到 `book/drafts/ch_003.v01/`：
草稿是上一版细纲的产物，场景 id 还对得上、内容却是照着旧要求写的，
留在原地会被 `--resume-drafts` 当成好草稿捡回去，等于细纲白改。

**checkpoint 同理有防呆**：`--graph` 续跑前会比对 checkpoint 里的细纲与手上
这份，不一致就换一条线程按新细纲重跑，并明确提示 —— 不会默默续上照着旧细纲
写的存档。

## 六条最容易被误改的设计

### 1. gate 配置里的数字和 prompt 里的数字**故意不一致**

`config/project.yaml` 的 `psychology_min: 28`，而 `WRITER_ROLE` 里写的目标是 45-95。

这不是笔误。**prompt 目标与实际产出有系统性落差**：让 writer 瞄准 45，它稳定
落在 29，而 29 正是作者读过后认可的密度。把 prompt 也降到 30，产出会掉到 20 以下。

prompt 里的是**瞄准点**，config 里的是**验收线**，本就不该相等。

### 2. writer 拿不到三份"设计期"skill

`WRITER_SKILLS` 里刻意没有 `character_design` / `romance_beats` / `campus_to_career`。
writer 一次只写一个 scene，拿到的是这些设计的**产物**（人物卡来自 state、
节拍类型在 scene spec 里），不需要知道它们怎么被设计出来。

剔掉后 writer 的 system_core 从 41K 降到 22K tokens。测试 `test_design_time_skills_kept_out_of_writer` 守着这条。

### 3. archivist **不收**全量设定集

实测事故：设定集 3,711 字符、正文 3,822 字符，两块体量相当且设定集在前，
模型归档了错的那一块 —— 摘要写成"开篇为人物设定集，无实际情节发生"，
还把已有四个人物用 `shen_zhiwei` 这类新 id 重新登记了一遍。

现在 archivist 的 `bible` 层是空的，花名册在 instruction 里。

### 4. 修订时必须给**完整文风剖面**，不能只给失败项

只给单项会打地鼠：第一轮修长句率，第二轮比喻就超标，第三轮对话又没了，
两轮上限内永远收敛不了。`GateReport.style_profile()` 会把通过的项也列出来 ——
模型要知道哪些不能动。

### 5. `max_chapter_summaries` 是**安全阀**，不是上下文窗口

`config/project.yaml` 里这个数（24）看着像"最近 N 章摘要进上下文"，**不是**。

进上下文的是「**尚未被卷梗概覆盖**的全部章节摘要」——分界线由卷末压缩来划，
不由一个与故事结构无关的数字来划。旧实现真的是"最近 10 章"，后果是：
一卷 18 章，写到第 11 章时第 1 章就掉出了视野，而那时本卷还没结束、卷梗概
还没产生，中间那八章谁都不管。

这个 24 只在**卷末压缩漏做**时才生效（窗口会一路长下去）。它一旦生效就是个
信号：某一卷的压缩没跑成，去补 `novel-agent compress <卷号>`。
**不要把它调小当成省 token 的手段**——那等于把上面那个洞又挖回来。

### 6. 不合格的产出**绝不覆盖**已有的合格章节

实测教训：一次 stitcher 崩了只吐 87 字，脚本照样写进最终路径，
把上一版 3,605 字的合格章节冲掉了，且无副本。

现在未通过的落 `book/needs_human/`，已有成稿保持不动。

## 两层摘要（2026-08-22 补齐）

方案第 3.3 节要的两层摘要，此前只有一层：`VolumeSummary` 有类型、architect 会读、
bible 会渲染，但**没有任何代码生产它**。现在补上了：

- `Archivist.compress_volume(state, volume)` —— 吃本卷全部章摘要 + 卷大纲 +
  未回收的债，吐一段 150-250 字的卷梗概。走 DeepSeek，一卷一次，成本 ≈ 0
- 触发点在**卷末那一章归档之后**，直调 pipeline 与走图两条路都接了
- 压缩失败**不连累这一章**：正文已经写完、检查过、归档了，为一次记账调用把
  它判失败是拿贵的赔便宜的。失败会在结果里留一条 note，补做用
  `novel-agent compress <卷号>`
- `StatePatch.volume_summary` 只在卷末填；逐章归档时模型顺手填的会被丢掉 ——
  否则它每章都能塞一段"本卷梗概"，把真正压缩出来的那份覆盖掉
- writer 现在也读得到**往卷梗概**（此前只读最近 N 章，往卷发生过什么完全不知道）。
  只给梗概，不给往卷的逐章摘要 —— 它一次只写一场，需要的是"别写出与前卷矛盾的
  东西"，细节该由细纲带进来

## 供应商现状（会变，用前先测）

| 渠道 | 用途 | 状态 |
|---|---|---|
| PackyAPI **cc-sale** | architect / writer / stitcher | 主力。号池间歇 403，**已配 403 重试 + 3.1 分钟退避窗口** |
| PackyAPI **aws-q** | 上面挂了的降级目标 | 同样的 claude-opus-5，另一个池子 |
| DeepSeek 官方直连 | judge / archivist / extractor | 283 次调用零故障，最稳 |
| PackyAPI **codex** | — | **禁止使用**。明文禁止二次开发并主动封锁非 Codex 客户端 |
| PackyAPI **azure-officially** | 想用 GPT 时的合法路径 | 倍率 5，比 cc-sale 贵 6.25 倍。已配好未启用 |

**cc-sale 的缓存现在是通的**（2026-08-21 复核 run_log：当天 41 次调用有 23 次
`cache_read` 非零，累计读回 110 万 token，比全不命中省 27%）。8-19 那次"`cache_read`
恒为 0"的结论**已经作废** —— 8-20 起每次调用都带缓存字段。`prompt_builder` 的
分层设计正在真实生效，别再按"缓存不生效"去做取舍。`config/models.yaml` 里那段
注释也一并改了。

成本按实测重算：第 2 章整轮 13 次调用 $0.30（writer 6 / stitcher 2 / judge 2 /
archivist 2 / architect 1），140 章外推约 $42 —— 比早先 $22 的估算高，因为那份
估算没算修订轮次。DeepSeek 那三个角色计价表没填，记 0，实际另有少量支出。

## 已知问题

- **RAG 检索质量差**。BM25 对具象词（地点/物件）有效，对抽象情绪无能为力，
  而场景规格里最有区分度的恰恰是情绪。默认关闭。换 embedding 只需替换
  `PassageIndex.score()` 一个方法。
- **第 2 章的对话占比已经解决**：成稿 21.8%，gate 全项通过（连同心理密度 37.1、
  比喻 28.6 都在区间内），没有用到容差。容差机制保留，但目前没有章节靠它放行。
  唯一擦边的是文风上沿 —— 第 2 章叙述句长中位 26（上限 27）、长句率 43.1%
  （上限 48%），比第 1 章（20 / 27.6%）明显长，**再往上走就该压一压了**。
- **正文草稿现在会落盘**：每写完一个场景就存进 `book/drafts/ch_XXX/`，缝合稿
  存成 `整章.md`，修订稿存成 `<场景id>.r1.md`（不覆盖初稿，两版能对着看）。
  起因是第 3 章第一次失败：三场写完、修订一轮又重写三场，缝合时上游 403 抛出来，
  $0.13 的正文一个字都没留下，连「对话占比为什么是 0」都查不了。第二次失败时
  这条已经生效，$0.07 的三场草稿保住了。
- **「对话占比 0.0%」的真正原因：ASCII 引号**（2026-08-22 拿到缝合稿后查实）。
  一度怀疑是 stitcher 把对话吃掉了，**这个怀疑是错的**：缝合稿实测对话
  15.6%，三场草稿加权 15.3%，字数保留率 101% —— stitcher 老老实实只调了接缝。
  真凶是 gate 的对话正则只认 `“”` 和 `「」`：模型偶尔吐 ASCII 引号
  （实测第 3 章缝合稿第 87 行有一处），那段就不算对话。首轮多半是整章的引号
  都成了 ASCII，于是对话占比读成 0.0%。
  **危害是误导修订**：gate 一边报「ASCII 引号」（真问题），一边报「对话占比
  0.0%，叙述压过了场景」（假问题），而后者会让 writer 去补根本不缺的对话。
  值得改：算对话占比时把 ASCII 引号那段也算进去，或者检到 ASCII 引号就
  不再报对话占比 —— 一次只让人修一个真问题。
- **章细纲以前不落盘**，跑完就没了 —— 上一版这里写着"下次跑之前先看一眼第 2 章
  细纲"，可当时根本没有东西可看。现已修：细纲在**生成的那一刻**就写到
  `book/outlines/ch_XXX.json/.md`（writer 中途崩掉也留得下来），并且多了
  `novel-agent outline N` 先出细纲给人看、`write --reuse-outline` 复用人看过的
  那份。独处场会在 md 里标出来（对话占比够不到下限，多半就是这个原因）。
- **本机装了透明文档加密软件（DLP）**。`grep`/`head`/`tail`/`wc` 在项目目录里
  读到的是密文，结果不可靠；**用 Python 读文件**。曾因此得到过假阴性的密钥扫描结果。

## judge 评分：从 2026-08-22 起有记录了

方案里写着「judge 阈值 24/35 是拍脑袋的起始值，跑 10 章后按实际分布调」。
但**分数此前跑完就丢** —— 前 5 次评审的七维分数一个都没留下，`run_log.jsonl`
里只有 token 和耗时。真到第 10 章，手上仍然是零个数据点。

现在每次评审追加一行到 `book/judgments.jsonl`：章号、七维分数、总分、最低分、
过没过、**当时的阈值**（阈值以后会改，不记下来旧数据没法解读）、字数、
被点名的场景。`novel-agent judgments` 看分布。

调阈值时看三件事：

- **哪一维永远高分** —— 那道线是摆设，没有区分度。（第 3 章七项全 4 分以上，
  而 judge 自己的 prompt 写着「七项全 4 分以上通常说明没读仔细」—— 是这章
  真好还是 judge 偏松，一章看不出来，十章的分布看得出来）
- **哪一维反复低分** —— 那是系统性弱项，该改 skills 或细纲，**不是调低阈值**。
  降标准让稿子通过是自欺
- **总分线落在分布的什么位置** —— 决定打回率。卡在中位数上就会一直烧修订，
  而每轮修订是 3 次 writer 加 1 次缝合

最终标尺是人不是分数：judge 是作者的代理，校准方式是作者读 3-5 章各给
「能用/不能用」，再把阈值设成机器判断与人一致。心理密度那条线当初就是这么定的。

`judgments.jsonl` 里现在有一行第 3 章的（31/35），是从 checkpoint 回捞的，
带 `source` 字段标着来源，与后面真正记录的行区分。

## 常用命令

```bash
.venv/bin/novel-agent status                    # 进度 / 成本 / 逾期的情感债
.venv/bin/novel-agent plan 2 19 36 毕业过渡      # 出卷大纲（要人工确认）
.venv/bin/novel-agent outline 3                 # 只出第 3 章细纲，人看完再写
.venv/bin/novel-agent write --reuse-outline --start 3   # 复用那份细纲，不重出
.venv/bin/novel-agent write --graph --start 3   # 走图，崩了能从崩的节点续跑
.venv/bin/novel-agent write --reuse-outline --resume-drafts --start 3  # 捡草稿接着跑
.venv/bin/novel-agent write -n 5                # 连写 5 章（细纲现出，仍会落盘）
.venv/bin/novel-agent check book/chapters/ch_001.md --chapter 1
.venv/bin/novel-agent judgments                 # judge 七维分布（调阈值的依据）
.venv/bin/novel-agent compress 1                # 补做卷末压缩
.venv/bin/python scripts/check_setup.py         # 供应商连通性 + 实调探测
.venv/bin/python scripts/style_fingerprint.py   # 量化文风指纹
```

## 快照

`snapshots/v1_low_psychology/`（心理下限 45）与 `snapshots/v2_planA/`（当前，含第 1 章正文）。
各带 README 说明差异与恢复方法。

作者手上另有一份更早的第 1 章（心理密度 12.9），**磁盘上无副本**。
